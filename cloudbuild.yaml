substitutions:
  _SERVICE_NAME: keycloak-client-svc-poc
  _REGION: asia-southeast1
  _PROJECT: active-bolt-478008-c9
  _IMAGE_NAME: asia-southeast1-docker.pkg.dev/${_PROJECT}/cdc-keycloak-poc/keycloak-client-svc-poc:latest

steps:
  # Step 1: Build Docker image using final JAR
  - name: 'gcr.io/cloud-builders/docker'
    env:
      - 'DOCKER_BUILDKIT=1'
    args:
      [
        'build',
        '-f', './src/main/docker/Dockerfile.native-micro',
        '-t', '${_IMAGE_NAME}',
        '.'
      ]

  # Step 3: Push Docker image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', '${_IMAGE_NAME}']

  # Step 4: Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      [
        'run', 'deploy', '${_SERVICE_NAME}',
        '--image', '${_IMAGE_NAME}',
        '--region', '${_REGION}',
        '--platform', 'managed',
        '--ingress', 'all',
        '--set-env-vars', 'USER_DATA_API_URL=http://10.148.0.2',
        '--port', '8081',
        '--allow-unauthenticated',
        '--project', '${_PROJECT}',
        '--network', 'default',
        '--subnet', 'default',
        '--vpc-egress', 'private-ranges-only',
        '--min-instances', '1',
        '--max-instances', '1'
      ]

images:
  - '${_IMAGE_NAME}'

options:
  logging: CLOUD_LOGGING_ONLY   # This avoids the log bucket error
  dynamicSubstitutions: true